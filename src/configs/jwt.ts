export const accessToken = process.env.ACCESS_TOKEN?.trim();
export const refreshToken = process.env.REFRESH_TOKEN?.trim();

export const ACCESS_TOKEN_EXPIRES_IN = "1h";
export const REFRESH_TOKEN_EXPIRES_IN = "7d";
